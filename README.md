# 🚀 Projeto: Conversor de Moedas SOLID

Projeto de Conversor de Moedas em Python com Tkinter, desenvolvido como parte da avaliação do curso de Análise e Desenvolvimento de Sistemas (TADS).

O foco principal foi a aplicação prática de conceitos de engenharia de software, incluindo:
* **Princípios SOLID:** (SRP e DIP) para desacoplamento de código.
* **Versionamento:** Uso de Git e GitHub para controle de versão.
* **Interface Gráfica:** Utilização da biblioteca nativa `tkinter`.
* **Consumo de API:** Busca de dados de uma API de câmbio em tempo real.

---

## 🛠️ Tecnologias Utilizadas
* Python 3
* Tkinter (GUI)
* Requests (para chamadas HTTP)

---

## ▶️ Como Executar

1.  **Clone o repositório:**
    ```bash
    git clone https://github.com/EliasGarciaDev/projeto-conversor-moedas-solid.git
    cd projeto-conversor-moedas-solid
    ```

2.  **(Recomendado) Crie um ambiente virtual:**
    ```bash
    python -m venv venv
    ```

3.  **Ative o ambiente:**
    * No Windows: `venv\Scripts\activate`
    * No Linux/Mac: `source venv/bin/activate`

4.  **Instale as dependências:**
    ```bash
    pip install -r requirements.txt
    ```

5.  **Execute a aplicação:**
    ```bash
    python main.py
    ```